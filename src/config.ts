// config.ts
export const VIDEO_CONFIG = {
  PROGRESS_BAR_START_TIME: 5, // Seconds before end when progress bar should start
  EXIT_FULLSCREEN_BEFORE_END: 7, // Seconds before end to exit fullscreen
  DEFAULT_VIDEO_LENGTH: 10, // 10 minutes in seconds
};
