// config.ts
export const VIDEO_CONFIG = {
  PROGRESS_BAR_START_TIME: 20, // Seconds before end when progress bar should start
  EXIT_FULLSCREEN_BEFORE_END: 5, // Seconds before end to exit fullscreen
  DEFAULT_VIDEO_LENGTH: 30, // 10 minutes in seconds
};
